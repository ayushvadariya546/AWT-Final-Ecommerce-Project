{
  "name": "pro1-workspace",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix ecommerce run dev\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix ecommerce run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}